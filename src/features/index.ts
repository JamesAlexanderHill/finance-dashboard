// Import each feature to trigger its registry.register() side-effect.
// Comment out a line to disable a feature (removes it from nav and plugin hooks).
import '~/features/core'
import '~/features/dashboard'
import '~/features/transactions'
import '~/features/categories'
